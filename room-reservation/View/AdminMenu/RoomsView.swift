import SwiftUI

struct RoomsView: View {
    var body: some View {
        VStack {
            Text("Rooms Section")
                .font(.title)
                .padding()

            // Add the content specific to rooms here
            Spacer()
        }
        .padding()
    }
}
