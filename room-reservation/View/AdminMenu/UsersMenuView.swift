import SwiftUI

struct UsersMenuView: View {
    var body: some View {
        VStack {
            Text("Users Section")
                .font(.title)
                .padding()

            // Add the content specific to reservations here
            Spacer()
        }
        .padding()
    }
}
